# Byte-compiled files and caches
__pycache__/
*.pyc
.ipynb_checkpoints/

# Virtual environments
venv*/

# Editor and local configuration
.vscode/
.claude/
.idea/
.python-version
.DS_Store

# Superseded or scratch files kept locally only
notebooks/tests.ipynb
notebooks/tests2.ipynb
notebooks/old_*.ipynb
simulation_toolbox/old_write_simulation_scripts.py
simulation_toolbox/original_marina.py

# Local data and trained emulator snapshots (too large for the repository)
simulation_toolbox/snapshot/
simulation_toolbox/rodero_healthy/

# Output of reproduce_figures.sh, and ranking files regenerated in place
results/
example_data/**/Rank_Si_total_max*.txt.new

# Manuscript material and working notes (local only)
simulation_toolbox/writing/
MAVACAMTEN_*.md
REVIEWER_RESPONSE_*.md
UPDATED_ABSTRACT.md

# Poetry lock file: not shipped, because the optional "simulation" extra points
# at private repositories that most users cannot resolve. Dependencies are
# resolved from pyproject.toml at install time.
poetry.lock
